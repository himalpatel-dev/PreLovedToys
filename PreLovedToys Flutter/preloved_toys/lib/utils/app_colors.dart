import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Green
  static const Color primary = Color(0xFF2F4B4E);

  // Light Grayish
  static const Color secondary = Color(0xFFBFC7C8);

  // Medium Gray Green
  static const Color medium = Color(0xFFA5B3B4);

  // Very Light
  static const Color light = Color(0xFFDCDEDE);

  // Text Colors
  static const Color textDark = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFF757575);
}
